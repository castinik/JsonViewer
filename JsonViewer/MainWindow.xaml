<Window x:Class="JsonViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JsonViewer"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<StackPanel Orientation="Horizontal" Margin="5">
			<Button Content="Apri JSON da File" Click="OpenJsonFile_Click" Width="150" Margin="0,0,10,0"/>
			<TextBlock x:Name="FilePathLabel" VerticalAlignment="Center"/>
		</StackPanel>

		<StackPanel Orientation="Horizontal" Margin="5" Grid.Row="1">
			<Button Content="Espandi Tutto" Click="EspandiTutto_Click" Margin="10"/>
			<Button Content="Collassa Tutto" Click="CollassaTutto_Click" Margin="10"/>
		</StackPanel>

		<!-- Barra di caricamento indeterminata -->
		<ProgressBar x:Name="LoadingBar"
                     IsIndeterminate="True"
                     Visibility="Collapsed"
                     Height="20"
                     Width="200"
                     HorizontalAlignment="Center"
                     VerticalAlignment="Top"
                     Margin="0,10,0,0" />

		<TreeView x:Name="JsonTreeView" 
          Margin="10"
		  Grid.Row="2"
          VirtualizingStackPanel.IsVirtualizing="True" 
          VirtualizingStackPanel.VirtualizationMode="Recycling"
          ScrollViewer.CanContentScroll="True">
</TreeView>
	</Grid>
</Window>
